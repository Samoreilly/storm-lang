


proc int sum(one: int, two: int) {
    return one + two;
}



proc int fibonaci(n: int) {
    if(n <= 1) {
        return n;
    }

    return fibonaci(n - 1) + fibonaci(n - 2);
}


proc void main() {

    echo(fibonaci(10));

    x: int = sum(1, 2);
    echo(x);
}

