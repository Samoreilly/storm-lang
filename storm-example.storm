

storm Node {
    x: int;
    y: int;
};



proc void main() {

    n : Node{1, 2};

    a: int = 10;
    b: int = 20;
    a = 30;
    echo(a);

    total: int = 0;

    fun(0, 0);


    for(i: int = 1; i < 350;i++) {
        result: int = 5;
        echo(result);
    }

}

proc void fun(var_one: int, var_two: int) {
    
    var_one += 1;
    var_two += var_two;
    echo(var_one);

    return fun(var_two, var_one);
}

