



proc void main() {

    if(1 < 5) {
        return 1;
        echo("hello");
    }

    num(2);

    total: int = 5;
    echo(total);

    x: int = fib(40);
    echo(x);

    y: int = fib(2);
    echo(y);
}

