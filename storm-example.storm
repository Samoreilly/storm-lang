


proc int add(a: int, b: int) {
    return a + b;
}

proc int run(n: int) {
    sum: int = 0;

    for(i: int = 0;i < n;i++) {
        sum = sum + i;
    }
    return sum;
}

proc void main() {
    for(i: int = 0;i < 50;i++) {
        echo(run(100000));
    }
}
